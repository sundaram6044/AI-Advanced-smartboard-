/* ===================================================================
   init.js
   Starts the app. Must load LAST, after every other file has defined
   its functions. Add any new "run once on startup" calls here.
   =================================================================== */

refreshPenUI();
resize();
renderPageStrip();
